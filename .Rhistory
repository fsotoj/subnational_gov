distinct() %>%
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE),
max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(sled_names), .direction = "down") %>%  # <- carry forward
ungroup()
data <- left_join(NED,SED,c("country_name","country_code","year")) %>%
left_join(.,SEED,c("country_state_code","year"))  %>%
select(-matches("\\.y$")) %>%
rename_with(~ gsub("\\.x$", "", .x), ends_with(".x")) %>%
left_join(.,SLED_filtered,c("country_state_code","year"))  %>%
select(-matches("\\.y$")) %>%
rename_with(~ gsub("\\.x$", "", .x), ends_with(".x"))
geom <- st_read("data/geom_simple_maps.geojson")
SLED
SLED %>% names()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# carry-forward within each chamber separately
group_by(country_state_code, chamber_sub_leg) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year) %>%
# carry-forward within each chamber separately
group_by(country_state_code, chamber_sub_leg) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year) %>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year) %>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup()
SLED_wide <- SLED %>%
# keep if you truly want only those elections
# filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
group_by(country_state_code, chamber_sub_leg) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
# filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
group_by(country_state_code, chamber_sub_leg) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
# ensure a single value per (country_state_code, chamber_sub_leg, year)
group_by(country_state_code, chamber_sub_leg, year, .add = TRUE) %>%
summarise(across(all_of(sled_names), ~ dplyr::last(., na.rm = TRUE)), .groups = "drop") %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide <- SLED %>%
# filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
group_by(country_state_code, chamber_sub_leg) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, chamber_sub_leg, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}",
values_fn   = list(.default = ~ dplyr::last(., na.rm = TRUE))  # <- collapse duplicates
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct()
SLED_wide %>% View()
data_info <- read.xlsx("data/dict_new.xlsx") %>%
select(Category= category, Variable = variable, Description = description)
dict <- read.xlsx("data/dict_new.xlsx") %>% filter(scope == "subnational")
sled_names <- dict %>%
filter(dataset == "SLED", viewable_map == 1) %>% pull(variable)
NED <- read.xlsx("data/NED (v.0.1).xlsx") %>%
mutate(ideo_party_nat_exe = as.double(ideo_party_nat_exe),
start_date_head_nat_exe = as.Date(start_date_head_nat_exe - 2, origin = "1900-01-01"),
end_date_head_nat_exe   = as.Date(end_date_head_nat_exe - 2, origin = "1900-01-01"))
SEED <- read.xlsx("data/SEED SHINY (v.0.1).xlsx")
SED <- read.xlsx("data/SED (v.0.1).xlsx")
SLED <- read.xlsx("data/SLED (v.0.1).xlsx")
CFTDFLD <- read.xlsx("data/CFTDFLD (v.0.1).xlsx")
SLED_filtered <- SLED %>%
filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, all_of(sled_names)) %>%
distinct() %>%
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE),
max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(sled_names), .direction = "down") %>%  # <- carry forward
ungroup()
data <- left_join(NED,SED,c("country_name","country_code","year")) %>%
left_join(.,SEED,c("country_state_code","year"))  %>%
select(-matches("\\.y$")) %>%
rename_with(~ gsub("\\.x$", "", .x), ends_with(".x")) %>%
left_join(.,SLED_filtered,c("country_state_code","year"))  %>%
select(-matches("\\.y$")) %>%
rename_with(~ gsub("\\.x$", "", .x), ends_with(".x"))
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
data_info <- read.xlsx("data/dict_new.xlsx") %>%
select(Category= category, Variable = variable, Description = description)
dict <- read.xlsx("data/dict_new.xlsx") %>% filter(scope == "subnational")
sled_names <- dict %>%
filter(dataset == "SLED", viewable_map == 1) %>% pull(variable)
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
#names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide <- SLED %>%
#filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
# now widen: one row per country_state_code-year, columns suffixed by 1/2
pivot_wider(
id_cols   = c(country_state_code, year),
names_from  = all_of(sled_names),
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}"
) %>%
arrange(country_state_code, year)
SLED_wide <- SLED %>%
# filter(chamber_election_sub_leg == 1) %>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}",
values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)
SLED_wide
SLED_wide %>% View()
SLED %>%
filter(chamber_election_sub_leg %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct()
SLED %>%
filter(country_state_code %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct()
SLED_wide <- SLED %>%
filter(country_state_code %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_sub_leg, all_of(sled_names)) %>%
distinct()
SLED_wide %>% View()
SLED_wide <- SLED %>%
filter(country_state_code %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_sub_leg, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_election_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)
sled_names
SLED_wide <- SLED %>%
filter(country_state_code %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_election_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)
SLED
SLED %>% names()
SLED_wide <- SLED %>%
filter(country_state_code %in% c("03206","03202"))%>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year),
names_from  = chamber_election_sub_leg,
values_from = all_of(sled_names),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_election_sub_leg,
values_from = c(all_of(sled_names),-chamber_sub_leg),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)
SLED_wide %>% View()
SLED_wide <- SLED %>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_election_sub_leg,
values_from = c(all_of(sled_names),-chamber_sub_leg),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)%>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup()
as.vector(outer(sled_names, c("_1","_2"), paste0))
as.vector(outer(c(sled_names,-chamber_sub_leg), c("_1","_2"), paste0))
as.vector(outer(c(sled_names,-"chamber_sub_leg"), c("_1","_2"), paste0))
sled_names
as.vector(outer(all_of(c(sled_names,-chamber_sub_leg)), c("_1","_2"), paste0))
sled_names[-"chamber_sub_leg"]
sled_names
sled_names[.,-"chamber_sub_leg"]
as.vector(outer(setdiff(sled_names, "chamber_sub_leg")), c("_1","_2"), paste0))
(sled_names, "chamber_sub_leg")
(sled_names, "chamber_sub_leg"))
setdiff(sled_names, "chamber_sub_leg")
as.vector(outer(setdiff(sled_names, "chamber_sub_leg"), c("_1","_2"), paste0))
outer(setdiff(sled_names, "chamber_sub_leg"), c("_1","_2"), paste0)
SLED_wide <- SLED %>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_election_sub_leg,
values_from = c(all_of(sled_names),-chamber_sub_leg),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)%>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(as.vector(outer(setdiff(sled_names, "chamber_sub_leg"), c("_1","_2"), paste0))), .direction = "down") %>%
ungroup()
SLED_wide %>% View()
cols_to_fill <- c("chamber_sub_leg",as.vector(outer(setdiff(sled_names,"chamber_sub_leg"), c("_1","_2"), paste0)))
cols_to_fill
cols_to_fill <- c("chamber_sub_leg",as.vector(outer(setdiff(sled_names,"chamber_sub_leg"), c("_1","_2"), paste0)))
SLED_wide <- SLED %>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_election_sub_leg,
values_from = c(all_of(sled_names),-chamber_sub_leg),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)%>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(sled_names), .direction = "down") %>%
ungroup()
SLED_wide <- SLED %>%
select(country_state_code, year, chamber_election_sub_leg, all_of(sled_names)) %>%
distinct() %>%
pivot_wider(
id_cols     = c(country_state_code, year,chamber_sub_leg),
names_from  = chamber_election_sub_leg,
values_from = c(all_of(sled_names),-chamber_sub_leg),
names_glue  = "{.value}_{chamber_election_sub_leg}",
#values_fn   = ~ dplyr::last(., na_rm = TRUE),   # collapse duplicates
values_fill = NA                                 # fill absent cells with NA
) %>%
arrange(country_state_code, year)%>%
# carry-forward within each chamber separately
group_by(country_state_code) %>%
complete(year = seq(min(year, na.rm = TRUE), max(year, na.rm = TRUE), 1)) %>%
arrange(country_state_code, year) %>%
fill(all_of(cols_to_fill), .direction = "down") %>%
ungroup()
SLED_wide %>% View()
# Build a JSTree where each dataset is a parent and each pretty_name is a child
get_jstree_data_vars <- function(dict) {
tree_data <- list()
datasets <- unique(dict$dataset)
for (ds in datasets) {
ds_node <- list(
id = ds,
text = ds,
children = list()
)
vars <- unique(dict$pretty_name[dict$dataset == ds])  # children per dataset
for (v in vars) {
var_node <- list(
# Keep a composite id like "DATASET-PRETTY_NAME" (same pattern as your first tree)
id = paste(ds, v, sep = "-"),
text = v
)
ds_node$children[[length(ds_node$children) + 1]] <- var_node
}
tree_data[[length(tree_data) + 1]] <- ds_node
}
return(jsonlite::toJSON(tree_data, auto_unbox = TRUE))
}
source("get_jstree_data_vars.R")
jstree_json_vars <- get_jstree_data_vars(dict)
jstree_json_vars
jstree_json_vars <- get_jstree_data_vars(dict %>% filter(viewable_map == 1))
runApp()
runApp()
cat(jstree_json_vars)
library(jsonlite)
library(htmltools)
jstree_json_vars <- get_jstree_data_vars(dict)
# Simple HTML wrapper
html <- tags$html(
tags$head(
tags$link(rel = "stylesheet", href = "https://cdnjs.cloudflare.com/ajax/libs/jstree/3.3.12/themes/default/style.min.css"),
tags$script(src = "https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"),
tags$script(src = "https://cdnjs.cloudflare.com/ajax/libs/jstree/3.3.12/jstree.min.js")
),
tags$body(
tags$div(id = "tree"),
tags$script(HTML(sprintf("
$(function() {
$('#tree').jstree({
'core': { 'data': %s }
});
});
", jstree_json_vars)))
)
)
# Open in Viewer or browser
html_print(html)
jstree_json_vars <- get_jstree_data_vars(dict %>% filter(viewable_map==1))
# Simple HTML wrapper
html <- tags$html(
tags$head(
tags$link(rel = "stylesheet", href = "https://cdnjs.cloudflare.com/ajax/libs/jstree/3.3.12/themes/default/style.min.css"),
tags$script(src = "https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"),
tags$script(src = "https://cdnjs.cloudflare.com/ajax/libs/jstree/3.3.12/jstree.min.js")
),
tags$body(
tags$div(id = "tree"),
tags$script(HTML(sprintf("
$(function() {
$('#tree').jstree({
'core': { 'data': %s }
});
});
", jstree_json_vars)))
)
)
# Open in Viewer or browser
html_print(html)
runApp()
runApp()
runApp()
shiny::runApp()
